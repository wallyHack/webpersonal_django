from django.apps import AppConfig


class PortfolioConfig(AppConfig):
    name = 'portfolio'
    # asigna un nombre personalizado a la app(Para el sitio administrativo)
    verbose_name = 'Portafolio'
